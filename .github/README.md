# GitHub Pages Deployment

This repository deploys to GitHub Pages with the `Deploy GitHub Pages` workflow.

Repository settings still need GitHub Pages configured to use **GitHub Actions**
as the source. After that, pushes to `main` publish `index.html`, `app.js`,
`styles.css`, `src/`, and `data/`.
